# Markdown Hub - Python 后端依赖
#
# 各功能所需依赖互不耦合，可按需安装。
# 最小安装仅需 PyMuPDF（保证 PDF → Markdown）。
# 详见 README.md "安装" 章节。

# ─────────────────────────────────────────
# 核心：PDF → Markdown（最小依赖）
# ─────────────────────────────────────────
PyMuPDF

# ─────────────────────────────────────────
# Word(.docx) → Markdown
# ─────────────────────────────────────────
docx2txt

# ─────────────────────────────────────────
# Excel → Markdown
# ─────────────────────────────────────────
pandas
openpyxl
tabulate
xlrd>=2.0.1

# ─────────────────────────────────────────
# PPTX → Markdown
# ─────────────────────────────────────────
python-pptx

# ─────────────────────────────────────────
# HTML → Markdown
# ─────────────────────────────────────────
html2text

# ─────────────────────────────────────────
# Markdown → DOCX（还需系统安装 Pandoc）
# ─────────────────────────────────────────
pandoc-attributes
python-docx>=0.8.11
docxtpl>=0.16.0,<0.21.0; sys_platform == 'win32'
docxtpl>=0.16.0; sys_platform == 'darwin' or sys_platform == 'linux'
docxcompose; sys_platform == 'win32' or sys_platform == 'darwin' or sys_platform == 'linux'
pywin32; sys_platform == 'win32'

# ─────────────────────────────────────────
# Markdown → PDF（还需系统安装 Pandoc）
# ─────────────────────────────────────────
markdown>=3.4.0

# ─────────────────────────────────────────
# Markdown → PPTX / 图表 → PNG
# ─────────────────────────────────────────
Pillow>=9.0.0

# ─────────────────────────────────────────
# PDF OCR 回退（扫描版 PDF 识别，可选）
# ─────────────────────────────────────────
pypdf
pytesseract
pdf2image

# ─────────────────────────────────────────
# SVG 转换（使用内置 Batik）
# ─────────────────────────────────────────
defusedxml
tinycss2
webencodings

# ─────────────────────────────────────────
# 基础 / 通用
# ─────────────────────────────────────────
lxml
cffi
chardet
pycparser
Jinja2
MarkupSafe
psutil
typing-extensions>=4.0.0

# macOS 特定
appscript; sys_platform == 'darwin'

# ─────────────────────────────────────────
# 系统级外部依赖（非 pip，需手动安装）
# ─────────────────────────────────────────
# 1. pandoc - Mac: brew install pandoc, Windows: https://pandoc.org/installing.html
# 2. Tesseract OCR - Mac: brew install tesseract, Windows: https://github.com/UB-Mannheim/tesseract/wiki
# 3. Java - 用于 SVG/PlantUML 转换（项目内置 Batik）
# 4. draw.io - https://github.com/jgraph/drawio-desktop/releases
# 5. Mermaid CLI (可选) - npm install -g @mermaid-js/mermaid-cli
